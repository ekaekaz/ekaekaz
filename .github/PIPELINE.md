<!-- Auto-generated by scripts/generate_pipeline_diagram.py — do not edit by hand. -->
# CI/CD pipeline

Diagram of every workflow in `.github/workflows/`, regenerated on each change by the `Pipeline diagram` action. Hexagon nodes gate on a required-reviewer environment (⏸).

```mermaid
flowchart LR
  classDef gated fill:#d98e4a22,stroke:#d98e4a,stroke-width:2px;
  classDef error fill:#e2665b22,stroke:#e2665b,stroke-width:2px;
  subgraph ci ["ci"]
    ci_trigger(["workflow_dispatch / push / pull_request"])
    ci_build["build\n4 steps"]
    ci_trigger --> ci_build
  end
  subgraph claude_code_review ["Claude PR Review"]
    claude_code_review_trigger(["pull_request (opened, synchronize)"])
    claude_code_review_review{{"review\n2 steps\n⏸ claude-review-approval"}}
    class claude_code_review_review gated;
    claude_code_review_trigger --> claude_code_review_review
  end
  subgraph maven_publish ["Maven Package"]
    maven_publish_trigger(["release (created)"])
    maven_publish_build["build\n4 steps"]
    maven_publish_trigger --> maven_publish_build
  end
```
